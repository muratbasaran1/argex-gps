{
  "name": "argex-gps-admin",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "node --check build.js",
    "test": "npm run lint",
    "dev": "npm run build && local-static-server dist --port 4173",
    "build": "node build.js"
  },
  "devDependencies": {
    "local-static-server": "file:../tools/local-static-server"
  }
}
